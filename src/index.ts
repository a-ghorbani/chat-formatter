import applyTemplate from './core/applyTemplate';
import { Conversation, Message, TemplateConfig } from './types';

export { applyTemplate, Conversation, Message, TemplateConfig };

// export templates (in case users want to customize)
import * as Templates from './templates';
export { Templates };
